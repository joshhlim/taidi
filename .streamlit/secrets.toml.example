# Copy to .streamlit/secrets.toml for local testing against Turso,
# or paste these two lines into the Streamlit Cloud secrets box.
# Without them, the app uses a local taidi.db file.
TURSO_DATABASE_URL = "libsql://your-db-name-your-org.turso.io"
TURSO_AUTH_TOKEN = "your-token-here"
